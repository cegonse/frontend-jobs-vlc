name: Tests

on: [push]

jobs:
  api:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Run Jest
      uses: stefanoeb/jest-action@1.0.3
      run: npm test
      working-directory: api
